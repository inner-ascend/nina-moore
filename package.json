{
  "name": "nina-moore",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://nina-moore.com",
  "description": "nina-moore.com — a hand-written static one-pager. No framework, no bundler.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && npx --yes serve dist -l 5173",
    "optimize-images": "node scripts/optimize-images.js"
  },
  "devDependencies": {
    "sharp": "^0.34.4"
  }
}
